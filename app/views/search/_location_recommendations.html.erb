<% @locations = Location.search(@query) %>
<% if !@locations.nil? && @locations.size > 0 %>
  <div id="locations">
    <h3>Recommended Locations</h3>
      <ol>
      <% for location in @locations %>
        <li><%= link_to "#{location.name.titleize}", location %><br />
          <%= location.address %></li>
      <% end %>
      </ol>
  </div>
<% end %>
